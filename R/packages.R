# packages.R
# These are the packages required to run the code in the functions.R, plan.R and
# make.R files. They're easily managed here in one central file.

library(drake)       # pipeline management
library(dplyr)       # data manipulation
library(ggplot2)     # plotting with grammar of graphics approach
library(govdown)     # GOV.UK-style RMarkdown
library(janitor)     # tidyconvenience functions
library(readODS)     # read open .ods files
library(rmarkdown)   # R-flavoured markdown for rendering to HTML, PDF, .docx, etc
library(tidyr)       # get data sets into 'tidy' format
library(visNetwork)  # Interactive plots
